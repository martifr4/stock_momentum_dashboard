# Dependencies for the crypto trading research system (crypto_research/).
# The older Reddit dashboard has its own minimal requirements.txt; this file is
# separate so the two projects' dependency sets stay clean.
pandas>=2.0
numpy>=1.24
pyarrow>=12.0        # Parquet cache
pyyaml>=6.0          # config loading
requests>=2.28       # Coinbase REST client
pytest>=7.0          # test suite

# Needed only for the LLM strategies (decision.combiner: "llm" or
# "claude_multisignal"). The rules strategy runs fully without either, and the
# LLM combiners degrade gracefully if the package/key is missing.
#   provider: anthropic  -> needs `anthropic`
#   provider: deepseek   -> needs `openai` (DeepSeek is OpenAI-compatible)
#   provider: openai     -> needs `openai`
anthropic>=0.40
openai>=1.30
